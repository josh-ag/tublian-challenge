export type RadioType = {
  name: string;
  tick: string;
  image: string;
  heading: string;
  text: string;
};

export type PaymentType = {
  type: string;
  name: string;
  heading: string;
  features: string[];
};
